if (!iCollection) {
  var iCollection = (function(window, undefined) {
    iCollection = {
      shop_url: '<%= @shop.url %>',
      markup: '<%= @shop.theme_markup %>'
    }

    console.log('Welcome to ' + iCollection.shop_url);
    console.log($(iCollection.markup));

    return iCollection;
  })(window);
}
